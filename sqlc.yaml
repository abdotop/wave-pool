version: "2"

sql:
  - engine: "sqlite"
    database:
       uri: "./wave-pool.db"
    schema: "db/migrations"
    queries: ["db/sqlc/queries.sql"]
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_empty_slices: true
        emit_interface: true
