# Goose configuration for migrations
# See: https://github.com/pressly/goose

# The directory containing migration files
migration_dir: db/migrations

dbstring: ./wave-pool.db
# Use sqlite3 driver
# Goose v3 now supports `sqlite3` via database/sql driver name.
driver: sqlite3
